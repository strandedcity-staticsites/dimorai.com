{
  "name": "hellojack-form-handler",
  "version": "1.0.0",
  "description": "Cloudflare Worker for handling HelloJack contact form submissions",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "wrangler dev --local"
  },
  "keywords": [
    "cloudflare-workers",
    "contact-form",
    "resend",
    "email-notifications"
  ],
  "author": "HelloJack",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}